default:
  combine_tagging: false
  control_regions: [cr_w, cr_t, cr_z]
  fixed_backgrounds: [other]
  signal_regions: [signal]
  systematics: [jer, c, b, u, t, trigger, somesyst]

combined:
  combine_tagging: true
  control_regions: [cr_w, cr_t, cr_z]
  # validation_regions: [signal]
  fixed_backgrounds: [other]
  signal_regions: [signal]
  systematics: [jer, c, b, u, t, trigger, somesyst]
